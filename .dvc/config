# La selección del almacenamiento del registro es específica de cada entorno.
# `vaaet-registry configure ...` escribe el remoto lógico `vaaet-registry` en
# .dvc/config.local, que permanece fuera de Git junto con sus credenciales.
